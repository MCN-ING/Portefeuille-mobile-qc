diff --git a/App/components/listItems/NotificationListItem.tsx b/App/components/listItems/NotificationListItem.tsx
index cb9e05b200554a8f401fc07ee1e2ae04fd2283ab..6eff159bd1881d9cf2d558d0f41e5c28e7ab504c 100644
--- a/App/components/listItems/NotificationListItem.tsx
+++ b/App/components/listItems/NotificationListItem.tsx
@@ -50,6 +50,8 @@ export interface NotificationListItemProps {
   notificationType: NotificationType
   notification: BasicMessageRecord | CredentialExchangeRecord | ProofExchangeRecord | CustomNotificationRecord
   customNotification?: CustomNotification
+  openSwipeableId?: string | null,
+  onOpenSwipeable?: (id: string | null) => void
 }
 
 type DisplayDetails = {
@@ -84,6 +86,8 @@ const NotificationListItem: React.FC<NotificationListItemProps> = ({
   notificationType,
   notification,
   customNotification,
+  openSwipeableId,
+  onOpenSwipeable,
 }) => {
   const navigation = useNavigation<StackNavigationProp<HomeStackParams>>()
   const [store, dispatch] = useStore()
diff --git a/App/screens/Connection.tsx b/App/screens/Connection.tsx
index 237e122dfe5537b74c9a40709b78a6867d9cabe8..f98586baef57d4c771e39130e57d2d60bedbcffb 100644
--- a/App/screens/Connection.tsx
+++ b/App/screens/Connection.tsx
@@ -51,7 +51,7 @@ const GoalCodes = {
 } as const
 
 const Connection: React.FC<ConnectionProps> = ({ navigation, route }) => {
-  const { oobRecordId, openIDUri, openIDPresentationUri, proofId, credentialId } = route.params
+  const { oobRecordId, openIDUri, openIDPresentationUri, proofId, credentialId, redirectQueryParam } = route.params
   const [
     logger,
     { useNotifications },
@@ -380,11 +380,11 @@ const Connection: React.FC<ConnectionProps> = ({ navigation, route }) => {
     }
 
     if (state.shouldShowProofComponent) {
-      return <ProofRequest proofId={proofId ?? state.notificationRecord.id} navigation={navigation} />
+      return <ProofRequest redirect={redirectQueryParam} proofId={proofId ?? state.notificationRecord.id} navigation={navigation} />
     }
 
     if (state.shouldShowOfferComponent) {
-      return <CredentialOffer credentialId={credentialId ?? state.notificationRecord.id} navigation={navigation} />
+      return <CredentialOffer redirect={redirectQueryParam} credentialId={credentialId ?? state.notificationRecord.id} navigation={navigation} />
     }
   }
 
diff --git a/App/screens/CredentialOffer.tsx b/App/screens/CredentialOffer.tsx
index 85c38333651e3a51517108d7d380d5fdf22df1ab..8d148820d046c6a8c13ccec10fefdb288dc3b934 100644
--- a/App/screens/CredentialOffer.tsx
+++ b/App/screens/CredentialOffer.tsx
@@ -41,9 +41,10 @@ import { ThemedText } from '../components/texts/ThemedText'
 type CredentialOfferProps = {
   navigation: any
   credentialId: string
+  redirect?: string
 }
 
-const CredentialOffer: React.FC<CredentialOfferProps> = ({ navigation, credentialId }) => {
+const CredentialOffer: React.FC<CredentialOfferProps> = ({ navigation, credentialId, redirect }) => {
   const { agent } = useAppAgent()
   const { t, i18n } = useTranslation()
   const { ColorPallet } = useTheme()
@@ -90,6 +91,16 @@ const CredentialOffer: React.FC<CredentialOfferProps> = ({ navigation, credentia
     },
   })
 
+  useEffect(() => {
+    if(redirect && credential && agent) {
+      const redirectMetadata = credential?.metadata.get('redirect')
+      if(!redirectMetadata?.deeplink){
+        credential.metadata.set('redirect', {'deeplink': redirect})
+        agent?.credentials.update(credential)
+      }
+    }
+  }, [redirect, credential, agent])
+
   useEffect(() => {
     const shouldShowTour = enableToursConfig && store.tours.enableTours && !store.tours.seenCredentialOfferTour
     if (shouldShowTour && screenIsFocused) {
diff --git a/App/screens/CredentialOfferAccept.tsx b/App/screens/CredentialOfferAccept.tsx
index 7849f912bff1ef14457948eb87eac951ad6c4932..3124140561de19fef213a65e4885b4a9c31adcaa 100644
--- a/App/screens/CredentialOfferAccept.tsx
+++ b/App/screens/CredentialOfferAccept.tsx
@@ -3,7 +3,7 @@ import { useCredentialById } from '@credo-ts/react-hooks'
 import { useNavigation } from '@react-navigation/native'
 import React, { useCallback, useEffect, useState } from 'react'
 import { useTranslation } from 'react-i18next'
-import { AccessibilityInfo, ScrollView, StyleSheet, View } from 'react-native'
+import { AccessibilityInfo, Linking, ScrollView, StyleSheet, View } from 'react-native'
 import { SafeAreaView } from 'react-native-safe-area-context'
 
 import Button, { ButtonType } from '../components/buttons/Button'
@@ -33,6 +33,7 @@ const CredentialOfferAccept: React.FC<CredentialOfferAcceptProps> = ({ visible,
   const [credentialDeliveryStatus, setCredentialDeliveryStatus] = useState<DeliveryStatus>(DeliveryStatus.Pending)
   const [timerDidFire, setTimerDidFire] = useState<boolean>(false)
   const [timer, setTimer] = useState<NodeJS.Timeout>()
+  const [redirect, setRedirect] = useState<string | undefined>(undefined)
   const credential = useCredentialById(credentialId)
   const navigation = useNavigation()
   const { ListItems } = useTheme()
@@ -65,6 +66,13 @@ const CredentialOfferAccept: React.FC<CredentialOfferAcceptProps> = ({ visible,
     },
   })
 
+  useEffect(() => {
+    const metadata = credential?.metadata.get('redirect')
+    if (metadata?.deeplink) {
+      setRedirect(metadata.deeplink)
+    }
+  }, [credential])
+
   if (!credential && !confirmationOnly) {
     throw new Error('Unable to fetch credential from Credo')
   }
@@ -171,12 +179,26 @@ const CredentialOfferAccept: React.FC<CredentialOfferAcceptProps> = ({ visible,
 
           {credentialDeliveryStatus === DeliveryStatus.Completed && (
             <View>
+              {redirect && (
+                <View style={{ marginBottom: 20 }}>
+                  <Button
+                    title={t('Global.Redirect')}
+                    accessibilityLabel={t('Global.Redirect')}
+                    testID={testIdWithKey('redirectDeeplink')}
+                    onPress={() => {
+                      Linking.openURL(redirect)
+                      onDoneTouched()
+                    }}
+                    buttonType={ButtonType.ModalPrimary}
+                  />
+                </View>
+              )}
               <Button
                 title={t('Global.Done')}
                 accessibilityLabel={t('Global.Done')}
                 testID={testIdWithKey('Done')}
                 onPress={onDoneTouched}
-                buttonType={ButtonType.ModalPrimary}
+                buttonType={redirect ? ButtonType.ModalSecondary: ButtonType.ModalPrimary}
               />
             </View>
           )}
diff --git a/App/screens/Home.tsx b/App/screens/Home.tsx
index 78cd401bd6b0e8e07d5d266274b0229403827462..332926b9c66da11f4fa561d99cd1ef99f316caa6 100644
--- a/App/screens/Home.tsx
+++ b/App/screens/Home.tsx
@@ -39,6 +39,8 @@ const Home: React.FC<HomeProps> = () => {
   const { start, stop } = useTour()
   const [showTourPopup, setShowTourPopup] = useState(false)
   const screenIsFocused = useIsFocused()
+  
+  const [notificationItemId, setNotificationItemId] = useState<string | null>(null)
 
   const styles = StyleSheet.create({
     flatlist: {
@@ -50,27 +52,49 @@ const Home: React.FC<HomeProps> = () => {
     (item: any): React.ReactNode => {
       let component: React.ReactNode
       if (item.type === 'BasicMessageRecord') {
-        component = <NotificationListItem notificationType={NotificationType.BasicMessage} notification={item} />
+        component = (
+          <NotificationListItem
+            openSwipeableId={notificationItemId}
+            onOpenSwipeable={setNotificationItemId}
+            notificationType={NotificationType.BasicMessage}
+            notification={item} />
+        )
       } else if (item.type === 'CredentialRecord') {
         let notificationType = NotificationType.CredentialOffer
         if (item.revocationNotification) {
           notificationType = NotificationType.Revocation
         }
-        component = <NotificationListItem notificationType={notificationType} notification={item} />
+        component = (
+          <NotificationListItem
+            openSwipeableId={notificationItemId}
+            onOpenSwipeable={setNotificationItemId}
+            notificationType={notificationType}
+            notification={item}
+          />
+        )
       } else if (item.type === 'CustomNotification' && customNotification) {
         component = (
           <NotificationListItem
+            openSwipeableId={notificationItemId}
+            onOpenSwipeable={setNotificationItemId}
             notificationType={NotificationType.Custom}
             notification={item}
             customNotification={customNotification}
           />
         )
       } else {
-        component = <NotificationListItem notificationType={NotificationType.ProofRequest} notification={item} />
+        component = (
+          <NotificationListItem
+            openSwipeableId={notificationItemId}
+            onOpenSwipeable={setNotificationItemId}
+            notificationType={NotificationType.ProofRequest}
+            notification={item}
+          />
+        )
       }
       return component
     },
-    [customNotification, NotificationListItem]
+    [customNotification, NotificationListItem, notificationItemId, setNotificationItemId]
   )
 
   useEffect(() => {
diff --git a/App/screens/ProofRequest.tsx b/App/screens/ProofRequest.tsx
index cd0a6a779cf9c9dcbaf5de4bf148c564fc4a8156..b44a904ce5fcea70651ecfb65996deed57d43c1b 100644
--- a/App/screens/ProofRequest.tsx
+++ b/App/screens/ProofRequest.tsx
@@ -70,6 +70,7 @@ import { ThemedText } from '../components/texts/ThemedText'
 type ProofRequestProps = {
   navigation: any
   proofId: string
+  redirect?: string
 }
 
 type CredentialListProps = {
@@ -79,7 +80,7 @@ type CredentialListProps = {
   missing: boolean
 }
 
-const ProofRequest: React.FC<ProofRequestProps> = ({ navigation, proofId }) => {
+const ProofRequest: React.FC<ProofRequestProps> = ({ navigation, proofId, redirect }) => {
   const { agent } = useAppAgent()
   const { t } = useTranslation()
   const { assertNetworkConnected } = useNetwork()
@@ -170,6 +171,16 @@ const ProofRequest: React.FC<ProofRequestProps> = ({ navigation, proofId }) => {
     },
   })
 
+  useEffect(() => {
+    if(redirect && proof && agent) {
+      const redirectMetadata = proof?.metadata.get('redirect')
+      if(!redirectMetadata?.deeplink){
+        proof.metadata.set('redirect', {'deeplink': redirect})
+        agent?.proofs.update(proof)
+      }
+    }
+  }, [redirect, proof, agent])
+
   useEffect(() => {
     if (!attestationMonitor) {
       return
diff --git a/App/screens/ProofRequestAccept.tsx b/App/screens/ProofRequestAccept.tsx
index a531eebb7403b8cb78a361b2d7020ce83c226601..bed9f6c29a5e3ce4c0d76ce66a157c19c2c58fca 100644
--- a/App/screens/ProofRequestAccept.tsx
+++ b/App/screens/ProofRequestAccept.tsx
@@ -3,7 +3,7 @@ import { useProofById } from '@credo-ts/react-hooks'
 import { useNavigation } from '@react-navigation/native'
 import React, { useEffect, useState } from 'react'
 import { useTranslation } from 'react-i18next'
-import { ScrollView, StyleSheet, View } from 'react-native'
+import { Linking, ScrollView, StyleSheet, View } from 'react-native'
 import { SafeAreaView } from 'react-native-safe-area-context'
 
 import Button, { ButtonType } from '../components/buttons/Button'
@@ -27,6 +27,7 @@ const ProofRequestAccept: React.FC<ProofRequestAcceptProps> = ({ visible, proofI
   const navigation = useNavigation()
   const { ColorPallet, TextTheme } = useTheme()
   const { SendingProof, SentProof } = useAnimatedComponents()
+  const [redirect, setRedirect] = useState<string | undefined>(undefined)
 
   const styles = StyleSheet.create({
     container: {
@@ -54,6 +55,13 @@ const ProofRequestAccept: React.FC<ProofRequestAcceptProps> = ({ visible, proofI
       marginTop: 20,
     },
   })
+  
+  useEffect(() => {
+    const metadata = proof?.metadata.get('redirect')
+    if (metadata?.deeplink) {
+      setRedirect(metadata.deeplink)
+    }
+  }, [proof])
 
   if (!proof && !confirmationOnly) {
     throw new Error('Unable to fetch proof from Credo')
@@ -116,6 +124,20 @@ const ProofRequestAccept: React.FC<ProofRequestAcceptProps> = ({ visible, proofI
 
         <View style={styles.controlsContainer}>
           <View>
+            {redirect && (
+              <View style={{ marginBottom: 20 }}>
+                <Button
+                  title={t('Global.Redirect')}
+                  accessibilityLabel={t('Global.Redirect')}
+                  testID={testIdWithKey('redirectDeeplink')}
+                  onPress={() => {
+                    Linking.openURL(redirect)
+                    onBackToHomeTouched()
+                  }}
+                  buttonType={ButtonType.ModalPrimary}
+                />
+              </View>
+            )}
             <Button
               title={t('Loading.BackToHome')}
               accessibilityLabel={t('Loading.BackToHome')}
